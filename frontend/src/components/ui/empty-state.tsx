/* empty-state.tsx intentionally removed — not imported by any page.
   Components EmptyState and ErrorState were defined here but never used.
   If needed in future, re-add with a single export pattern. */
